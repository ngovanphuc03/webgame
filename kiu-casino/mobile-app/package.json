{
    "name": "royal-casino-app",
    "version": "1.0.0",
    "description": "Royal Casino Mobile App",
    "main": "index.js",
    "scripts": {
        "build": "npx cap sync && cd android && gradlew assembleDebug",
        "build:release": "npx cap sync && cd android && gradlew assembleRelease"
    },
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/core": "^6.0.0",
        "@capacitor/splash-screen": "^6.0.0",
        "@capacitor/status-bar": "^6.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^6.0.0",
        "typescript": "^5.9.3"
    }
}
